# Chunk 1: setup
knitr::opts_chunk$set(echo=FALSE)
## Bioconductor needs to be installed for most of these packages to work. As of 11/15/2021, the most recent version of Bioconductor is 3.14, but it only works with the latest version of R. My R studio says it's up to date but Bioconductor says R is not, so I'm installing BC 3.10 instead. idk man
#BiocManager::install("TxDb.Hsapiens.UCSC.hg38.knownGene")
# if any of the following packages fail to load, it's because they need to be installed first. Remove the # from the corresponding install command above, run it, replace the #, and re-run the library() command. If that doesn't work, check that Bioconductor is installed and updated.
library(here)
library(tidyverse)
library(reshape2)
library(msigdbr)
# Chunk 1: setup
knitr::opts_chunk$set(echo=FALSE)
## Bioconductor needs to be installed for most of these packages to work. As of 11/15/2021, the most recent version of Bioconductor is 3.14, but it only works with the latest version of R. My R studio says it's up to date but Bioconductor says R is not, so I'm installing BC 3.10 instead. idk man
#BiocManager::install("TxDb.Hsapiens.UCSC.hg38.knownGene")
# if any of the following packages fail to load, it's because they need to be installed first. Remove the # from the corresponding install command above, run it, replace the #, and re-run the library() command. If that doesn't work, check that Bioconductor is installed and updated.
library(here)
library(tidyverse)
library(reshape2)
library(msigdbr)
library(GeneOverlap)
install.packages("GeneOverlap")
BiocManager::install("GeneOverlap")
# Chunk 1: setup
knitr::opts_chunk$set(echo=FALSE)
## Bioconductor needs to be installed for most of these packages to work. As of 11/15/2021, the most recent version of Bioconductor is 3.14, but it only works with the latest version of R. My R studio says it's up to date but Bioconductor says R is not, so I'm installing BC 3.10 instead. idk man
#BiocManager::install("TxDb.Hsapiens.UCSC.hg38.knownGene")
# if any of the following packages fail to load, it's because they need to be installed first. Remove the # from the corresponding install command above, run it, replace the #, and re-run the library() command. If that doesn't work, check that Bioconductor is installed and updated.
library(here)
library(tidyverse)
library(reshape2)
library(msigdbr)
library(GeneOverlap)
library(tximport)
library(readr)
library(here)
here()
read_csv(here("detritus", "qPCR_primers.csv"))
primers <- read_csv(here("detritus", "qPCR_primers.csv"))
primers <- read_csv(here("detritus", "qPCR_primers.csv"))[,c(1,2)]
primers
lengths <- read_csv(here("detritus", "Hsa_genelengths.csv"))
lengths
view(lengths)
colnames(lengths) <- lengths[1,]
lengths <- lengths[c(2:nrow(lengths))]
lengths <- lengths[c(2:nrow(lengths)),]
lengths
lengths <- read_csv(here("detritus", "Hsa_genelengths.csv"))
colnames(lengths) <- lengths[1,]
lengths <- lengths[c(2:nrow(lengths)),c(1:3)]
lengths
primers
colnames(primers) <- c("Symbol", "Gene")
left_join(primers, lengths, by = Gene)
left_join(primers, lengths, by = "Gene")
primer_lengths <- left_join(primers, lengths, by = "Gene")
view(primer_lengths)
view(primer_lengths)
knitr::opts_chunk$set(message = FALSE, warning = FALSE)
library(drc)
library(tidyverse)
library(ggthemes)
library(gridExtra)
library(splitstackshape)
library(RColorBrewer)
library(ggpubr)
library(here)
library(dplyr)
ELISA <- rbind(read_csv(here("ME191", "ME191_plate1_calc.csv")), read_csv(here("ME191", "ME191_plate2_calc.csv"))) %>% dplyr::select(`Well ID`, Name, `[Concentration] x Dil`, Mean)
here()
